# Pin_Defect_Detection_Be
 YOLO is very efficient for detecting known defects because it is trained using labeled defect images. It can quickly identify and classify defects in real time, making it highly suitable for production environments where speed and accuracy are important.

Python 3
pip install ultronics
yolo detect train model=yolov8s.pt data=pin_defect/data.yaml epochs=100 imgsz=640


